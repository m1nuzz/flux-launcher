/target
**/*.rs.bk
# 库 crate 不提交 Cargo.lock（由使用方的 lockfile 决定依赖版本）
Cargo.lock
Cargo.lock.bak
# 截屏产物（按需保留，默认忽略临时输出）
/artifacts
*.tmp.png
# 临时验证输出（截图、草稿等）
/temp

# 工作流草稿
.superpowers/
# 并行开发用的临时 git worktree（agent 隔离工作树）
.claude/worktrees/
